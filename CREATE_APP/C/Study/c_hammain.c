#include <stdio.h> // Thư viện chuẩn hỗ trợ các hàm nhập/xuất như printf

int main() { // Hàm chính, nơi chương trình bắt đầu thực thi
    printf("Hello World"); // In chuỗi ký tự "Hello World" ra màn hình
    return 0; // Trả về giá trị 0, báo hiệu chương trình kết thúc thành công
}
