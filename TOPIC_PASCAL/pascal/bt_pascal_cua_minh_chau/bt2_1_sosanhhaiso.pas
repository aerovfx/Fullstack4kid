{bt2.1_so sanh hai so a va b}
uses crt;
var a,b:integer;
begin
    clrscr;
    write('nhap vao so a ');readln(a);
    write('nhap vao so b ');readln(b);
    if a>b then write('a la so lon hon b')
    else write('b la so lon hon a');
    readln;
end.

